import { Avatar } from "@mantine/core"
import type { AvatarProps } from "@mantine/core"


const TamsAvatar = (props: AvatarProps) => {
  return (
    <Avatar {...props} />
  )
}

export default TamsAvatar
