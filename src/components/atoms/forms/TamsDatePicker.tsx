import { DatePickerInput } from '@mantine/dates'

type TamsDatePickerProps = DatePickerInput.Props

const TamsDatePickerInput = (props: TamsDatePickerProps) => {
  return <DatePickerInput {...props} />
}

export default TamsDatePickerInput
