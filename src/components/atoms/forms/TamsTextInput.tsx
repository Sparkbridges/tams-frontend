import { TextInput } from '@mantine/core'
type TamsTextInputProps = TextInput.Props

const TamsTextInput = (props: TamsTextInputProps) => {
  return <TextInput {...props} />
}

export default TamsTextInput
