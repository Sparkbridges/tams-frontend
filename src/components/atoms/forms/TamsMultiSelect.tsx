import { MultiSelect } from "@mantine/core"

type Props = MultiSelect.Props

const TamsMultiSelect = (props: Props) => {
  return (
    <MultiSelect
      {...props}
    />
  )
}

export default TamsMultiSelect
