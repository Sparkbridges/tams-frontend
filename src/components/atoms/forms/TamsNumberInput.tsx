import { NumberInput } from '@mantine/core'
import type { NumberInputProps } from '@mantine/core'

type TamsNumberInputProps = NumberInputProps

const TamsNumberInput = (props: TamsNumberInputProps) => {
  return <NumberInput {...props} />
}

export default TamsNumberInput
